# f-6 — binance-trading-toolkit 補上 GitHub Actions 自動測試

- PR：[columnbb/binance-trading-toolkit#2](https://github.com/columnbb/binance-trading-toolkit/pull/2)
- squash 合併：`cc89a39`（main）
- CI：pytest SUCCESS（run 36209079750）；本機 65 passed
- Perplexity：首審 **PASS**
- 對應待辦：機隊工程進度 `f-6`；登記冊無對應項目
- 部署：不需要（只新增 `.github/workflows/test.yml`）

## 做了什麼

趨勢、動能真倉程式都釘這個 repo，但它沒有 CI。新增 workflow：push main/development 與所有 PR 時以 Python 3.11 安裝 `.[dev]` 跑 pytest。

## 審閱

PASS。附註：`pull_request` 未限目標分支（刻意，所有 PR 都跑）；審閱端未取得完整 job 日誌，不把本機 65 passed 當作 CI 核實數。

## 隔離聲明

未碰正式機、開發機、交易所、Google 或實機帳本。完整差異見同名 `.patch`。
